import { StyleSheet, Text, View } from 'react-native';
import GsheetChargers from './GsheetChargers';
import GsheetChargers1 from './GsheetChargers1';

export default function App() {
  return (
            <GsheetChargers1/>
    
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#fff',
    alignItems: 'center',
    justifyContent: 'center',
  },
});
